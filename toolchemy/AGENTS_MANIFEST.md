# Toolchemy capability manifest

Auto-generated by `make docs-agents`. Do not edit by hand. Entries marked `(inferred)` lack a docstring and were derived from the signature.

Consumer agents: prefer reusing the symbols below over reimplementing logging, caching, retries, prompt iteration, tracker plumbing, or LLM client wiring.

## `toolchemy.ai.clients`

- **class** `ChatMessage(...)` — (inferred) Class `ChatMessage` extends dict.
  - `from toolchemy.ai.clients import ChatMessage`
- **class** `ChatMessages(...)` — (inferred) Class `ChatMessages` extends dict.
  - `from toolchemy.ai.clients import ChatMessages`
- **class** `DummyModelClient(name: str = 'dummy-model', fixed_response: str | None = None, system_prompt: str | None = None, default_model_config: toolchemy.ai.clients.common.ModelConfig | None = None, disable_cache: bool = True, log_level: int = 20)` — Client that answers locally instead of calling a model, for tests and offline runs.
  - `from toolchemy.ai.clients import DummyModelClient`
- **class** `ILLMClient()` — (inferred) Class `ILLMClient` extends ABC; methods: completion, completion_json, embeddings, invalidate_completion_cache, model_config….
  - `from toolchemy.ai.clients import ILLMClient`
- **class** `LLMCacheDoesNotExist(...)` — (inferred) Class `LLMCacheDoesNotExist` extends Exception.
  - `from toolchemy.ai.clients import LLMCacheDoesNotExist`
- **class** `LLMClientBase(default_model_name: str | None = None, default_embedding_model_name: str | None = None, default_model_config: toolchemy.ai.clients.common.ModelConfig | None = None, system_prompt: str | None = None, keep_chat_session: bool = False, retry_attempts: int = 5, retry_min_wait: int = 2, retry_max_wait: int = 60, truncate_log_messages_to: int = 200, fix_malformed_json: bool = True, cacher: toolchemy.utils.cacher.common.ICacher | None = None, disable_cache: bool = False, log_level: int = 20)` — (inferred) Class `LLMClientBase` extends ILLMClient, ICollectable, ABC; methods: collect, completion, completion_json, embeddings, invalidate_completion_cache….
  - `from toolchemy.ai.clients import LLMClientBase`
- **class** `ModelConfig(*, model_name: str | None = None, max_new_tokens: int | None = 2000, presence_penalty: float | None = 0.0, temperature: float = 0.7, top_p: float = 1.0) -> None` — (inferred) Class `ModelConfig` extends BaseModel; methods: copy, dict, json, model_copy, model_dump….
  - `from toolchemy.ai.clients import ModelConfig`
- **class** `ModelResponseError(...)` — (inferred) Class `ModelResponseError` extends Exception.
  - `from toolchemy.ai.clients import ModelResponseError`
- **class** `OllamaClient(uri: str, model_name: str | None = None, embedding_model_name: str | None = 'nomic-embed-text', default_model_config: toolchemy.ai.clients.common.ModelConfig | None = None, system_prompt: str | None = None, keep_chat_session: bool = False, think: bool | None = False, retry_attempts: int = 5, retry_min_wait: int = 2, retry_max_wait: int = 60, truncate_log_messages_to: int = 200, fix_malformed_json: bool = True, cacher: toolchemy.utils.cacher.common.ICacher | None = None, disable_cache: bool = False, log_level: int = 20)` — (inferred) Class `OllamaClient` extends LLMClientBase; methods: collect, completion, completion_json, embeddings, invalidate_completion_cache….
  - `from toolchemy.ai.clients import OllamaClient`
- **class** `OpenAIClient(api_key: str, model_name: str = 'gpt-3.5-turbo', embedding_model_name: str = 'text-embedding-3-large', default_model_config: toolchemy.ai.clients.common.ModelConfig | None = None, system_prompt: str | None = None, keep_chat_session: bool = False, no_cache: bool = False)` — (inferred) Class `OpenAIClient` extends BaseOpenAIClient; methods: collect, completion, completion_json, embeddings, invalidate_completion_cache….
  - `from toolchemy.ai.clients import OpenAIClient`
- **function** `create_llm(name: str, uri: str | None = None, api_key: str | None = None, default_model_config: toolchemy.ai.clients.common.ModelConfig | None = None, system_prompt: str | None = None, log_level: int = 20, no_cache: bool = False) -> toolchemy.ai.clients.common.LLMClientBase` — (inferred) Create llm — takes `name: str`, `uri: str | None`, `api_key: str | None`, `default_model_config: toolchemy.ai.clients.common.ModelConfig | None`, `system_prompt: str | None`, `log_level: int`, `no_cache: bool`; returns `LLMClientBase`.
  - `from toolchemy.ai.clients import create_llm`
- **function** `prepare_chat_messages(prompt: str, system_prompt: str | None = None, images_base64: list[str] | None = None, messages_history: list[toolchemy.ai.clients.common.ChatMessage] | None = None, envelope: bool = False) -> list[toolchemy.ai.clients.common.ChatMessage] | toolchemy.ai.clients.common.ChatMessages` — (inferred) Prepare chat messages — takes `prompt: str`, `system_prompt: str | None`, `images_base64: list[str] | None`, `messages_history: list[toolchemy.ai.clients.common.ChatMessage] | None`, `envelope: bool`; returns `list[toolchemy.ai.clients.common.ChatMessage] | toolchemy.ai.clients.common.ChatMessages`.
  - `from toolchemy.ai.clients import prepare_chat_messages`

## `toolchemy.ai.clients.common`

- **class** `ChatMessageContent(...)` — (inferred) Class `ChatMessageContent` extends dict.
  - `from toolchemy.ai.clients.common import ChatMessageContent`
- **class** `Usage(input_tokens: int, output_tokens: int, duration: float, cached: bool = False) -> None` — Usage(input_tokens: int, output_tokens: int, duration: float, cached: bool = False)
  - `from toolchemy.ai.clients.common import Usage`

## `toolchemy.ai.clients.openai_client`

- **class** `AzureOpenAIClient(api_key: str, api_endpoint: str, api_version: str, model_name: str = 'gpt-3.5-turbo', embedding_model_name: str = 'text-embedding-3-large', default_model_config: toolchemy.ai.clients.common.ModelConfig | None = None, system_prompt: str | None = None, keep_chat_session: bool = False)` — (inferred) Class `AzureOpenAIClient` extends BaseOpenAIClient; methods: collect, completion, completion_json, embeddings, invalidate_completion_cache….
  - `from toolchemy.ai.clients.openai_client import AzureOpenAIClient`
- **class** `BaseOpenAIClient(default_model_name: str | None = None, default_embedding_model_name: str | None = None, default_model_config: toolchemy.ai.clients.common.ModelConfig | None = None, system_prompt: str | None = None, keep_chat_session: bool = False, retry_attempts: int = 5, retry_min_wait: int = 2, retry_max_wait: int = 60, truncate_log_messages_to: int = 200, fix_malformed_json: bool = True, cacher: toolchemy.utils.cacher.common.ICacher | None = None, disable_cache: bool = False, log_level: int = 20)` — (inferred) Class `BaseOpenAIClient` extends LLMClientBase, ABC; methods: collect, completion, completion_json, embeddings, invalidate_completion_cache….
  - `from toolchemy.ai.clients.openai_client import BaseOpenAIClient`

## `toolchemy.ai.clients.pricing`

- **class** `Pricing()` — (inferred) Class `Pricing`.
  - `from toolchemy.ai.clients.pricing import Pricing`

## `toolchemy.ai.clients.whisper_client`

- **class** `WhisperClient(url: str, timeout: int | None = 30, log_level: int = 20)` — (inferred) Class `WhisperClient`; methods: transcribe.
  - `from toolchemy.ai.clients.whisper_client import WhisperClient`
- **function** `main(argv: list)` — (inferred) Main — takes `argv: list`.
  - `from toolchemy.ai.clients.whisper_client import main`

## `toolchemy.ai.prompting`

- **class** `IPromptOptimizer()` — (inferred) Class `IPromptOptimizer` extends ABC; methods: refactor.
  - `from toolchemy.ai.prompting import IPromptOptimizer`
- **class** `IPrompter()` — (inferred) Class `IPrompter` extends ABC; methods: create_template, delete, render, run_studio, template.
  - `from toolchemy.ai.prompting import IPrompter`
- **class** `PrompterBase(default_system_prompt: str | None = None, prompt_optimizer: toolchemy.ai.prompting.common.IPromptOptimizer | None = None, cacher: toolchemy.utils.cacher.common.ICacher | None = None, no_cache: bool = False, log_level: int = 20)` — (inferred) Class `PrompterBase` extends IPrompter, ABC; methods: create_template, delete, render, run_studio, template.
  - `from toolchemy.ai.prompting import PrompterBase`
- **class** `PrompterMLflow(default_system_prompt: str | None = None, registry_store_dir: str | None = None, prompt_optimizer: toolchemy.ai.prompting.common.IPromptOptimizer | None = None, cacher: toolchemy.utils.cacher.common.ICacher | None = None, no_cache: bool = False, log_level: int = 20)` — (inferred) Class `PrompterMLflow` extends PrompterBase; methods: create_template, delete, render, run_studio, template.
  - `from toolchemy.ai.prompting import PrompterMLflow`
- **class** `SimpleLLMPromptOptimizer(llm: toolchemy.ai.clients.common.ILLMClient, target_model_name: str | None = None, log_level: int = 20)` — (inferred) Class `SimpleLLMPromptOptimizer` extends IPromptOptimizer; methods: refactor.
  - `from toolchemy.ai.prompting import SimpleLLMPromptOptimizer`

## `toolchemy.ai.prompting.common`

- **class** `InvalidPromptError(...)` — (inferred) Class `InvalidPromptError` extends Exception.
  - `from toolchemy.ai.prompting.common import InvalidPromptError`
- **class** `Prompt(system: str | None = None, user: str | None = None, template_system: str | None = None, template_user: str | None = None) -> None` — Prompt(system: str | None = None, user: str | None = None, template_system: str | None = None, template_user: str | None = None)
  - `from toolchemy.ai.prompting.common import Prompt`

## `toolchemy.ai.prompting.prompter_mlflow`

- **function** `run_studio(registry_path: str | None = None)` — (inferred) Run studio — takes `registry_path: str | None`.
  - `from toolchemy.ai.prompting.prompter_mlflow import run_studio`
- **function** `testing()` — (inferred) Testing — takes no arguments.
  - `from toolchemy.ai.prompting.prompter_mlflow import testing`

## `toolchemy.ai.trackers`

- **class** `ITracker()` — (inferred) Class `ITracker` extends ABC; methods: disable, end_run, get_data, get_traces, log….
  - `from toolchemy.ai.trackers import ITracker`
- **class** `InMemoryTracker(experiment_name: str = 'dummy', disabled: bool = False)` — (inferred) Class `InMemoryTracker` extends TrackerBase; methods: disable, end_run, get_avg_metric_value, get_data, get_max_metric_value….
  - `from toolchemy.ai.trackers import InMemoryTracker`
- **class** `MLFlowTracker(tracking_uri: str, experiment_name: str, with_artifact_logging=True, registry_uri: str | None = None, tracking_client: mlflow.tracking.client.MlflowClient | None = None)` — (inferred) Class `MLFlowTracker` extends TrackerBase; methods: disable, end_run, get_avg_metric_value, get_data, get_max_metric_value….
  - `from toolchemy.ai.trackers import MLFlowTracker`
- **class** `NeptuneAITracker(project_name: str, experiment_name: str, api_token: str, with_artifact_logging: bool = True, disabled: bool = False)` — (inferred) Class `NeptuneAITracker` extends TrackerBase; methods: disable, end_run, get_avg_metric_value, get_data, get_id….
  - `from toolchemy.ai.trackers import NeptuneAITracker`

## `toolchemy.ai.trackers.common`

- **class** `TrackerBase(experiment_name: str, with_artifact_logging: bool = True, disabled: bool = False)` — (inferred) Class `TrackerBase` extends ITracker, ABC; methods: disable, end_run, get_avg_metric_value, get_data, get_max_metric_value….
  - `from toolchemy.ai.trackers.common import TrackerBase`

## `toolchemy.ai.trackers.mlflow_tracker`

- **function** `play()` — (inferred) Play — takes no arguments.
  - `from toolchemy.ai.trackers.mlflow_tracker import play`

## `toolchemy.db`

- **class** `Filter(key: str, value: Any, op: toolchemy.db.lightdb.FilterOp = <FilterOp.EQUAL: 5>) -> None` — Filter(key: str, value: Any, op: toolchemy.db.lightdb.FilterOp = <FilterOp.EQUAL: 5>)
  - `from toolchemy.db import Filter`
- **class** `FilterOp(*values)` — (inferred) Class `FilterOp` extends Enum.
  - `from toolchemy.db import FilterOp`
- **class** `ILightDB()` — (inferred) Class `ILightDB` extends ABC; methods: all, insert, insert_batch, remove, retrieve….
  - `from toolchemy.db import ILightDB`
- **class** `LightTinyDB(db_file_path: str, indexes: list[str] | None = None, log_level: int = 20, cacher: toolchemy.utils.cacher.common.ICacher | None = None)` — (inferred) Class `LightTinyDB` extends ILightDB; methods: all, insert, insert_batch, remove, retrieve….
  - `from toolchemy.db import LightTinyDB`
- **class** `NotFoundError(...)` — (inferred) Class `NotFoundError` extends Exception.
  - `from toolchemy.db import NotFoundError`

## `toolchemy.nlp`

- **function** `clean_text(text: str) -> str` — (inferred) Clean text — takes `text: str`; returns `str`.
  - `from toolchemy.nlp import clean_text`

## `toolchemy.utils`

- **class** `AtExitCollector()` — (inferred) Class `AtExitCollector`.
  - `from toolchemy.utils import AtExitCollector`
- **class** `CacheEntryDoesNotExistError(...)` — (inferred) Class `CacheEntryDoesNotExistError` extends Exception.
  - `from toolchemy.utils import CacheEntryDoesNotExistError`
- **class** `Cacher(name: str | None = None, cache_base_dir: Optional[str] = None, shards: int = 1, timeout: int = 30, thread_safe: bool = False, disabled: bool = False, log_level: int = 20)` — (inferred) Class `Cacher` extends CacherDiskcache; methods: collect, create_cache_key, exists, get, hash….
  - `from toolchemy.utils import Cacher`
- **class** `CacherPickle(name: str | None = None, cache_base_dir: Optional[str] = None, disabled: bool = False, log_level: int = 20, enable_thread_safeness: bool = False)` — Cacher implementation where cache is stored as a pickled local file
  - `from toolchemy.utils import CacherPickle`
- **class** `DummyCacher(with_memory_store: bool = False)` — (inferred) Class `DummyCacher` extends BaseCacher; methods: collect, create_cache_key, exists, get, hash….
  - `from toolchemy.utils import DummyCacher`
- **class** `ICacher()` — Cacher interface
  - `from toolchemy.utils import ICacher`
- **class** `ICollectable()` — (inferred) Class `ICollectable` extends ABC; methods: collect, label.
  - `from toolchemy.utils import ICollectable`
- **class** `Locations(prefix_dirs: dict | None = None, root_path: str | None = None, objective_path_mode: bool = False) -> None` — (inferred) Class `Locations`; methods: abs, in_, in_data, in_resources, in_root….
  - `from toolchemy.utils import Locations`
- **class** `Timer(started: bool = True)` — (inferred) Class `Timer`; methods: reset, tap.
  - `from toolchemy.utils import Timer`
- **function** `ff(fval: float | list[float] | int | list[int] | dict | str | numpy.float32, precision: int = 2)` — (inferred) Ff — takes `fval: float | list[float] | int | list[int] | dict | str | numpy.float32`, `precision: int`.
  - `from toolchemy.utils import ff`
- **function** `get_logger(name: str | None = None, level: int = 20, log_dir: str | None = None, with_time: bool = True, with_module_name: bool = True, with_log_level: bool = True, short_module_name: bool = False, say_hi: bool = False) -> logging.Logger` — (inferred) Get logger — takes `name: str | None`, `level: int`, `log_dir: str | None`, `with_time: bool`, `with_module_name: bool`, `with_log_level: bool`, `short_module_name: bool`, `say_hi: bool`; returns `Logger`.
  - `from toolchemy.utils import get_logger`
- **function** `hash_dict(input_dict: dict) -> str` — (inferred) Hash dict — takes `input_dict: dict`; returns `str`.
  - `from toolchemy.utils import hash_dict`
- **function** `pp(msg: str | dict | float | int | list, skip_fields: list | None = None, print_msg: bool = True) -> str` — (inferred) Pp — takes `msg: str | dict | float | int | list`, `skip_fields: list | None`, `print_msg: bool`; returns `str`.
  - `from toolchemy.utils import pp`
- **function** `pp_cast(msg: Any, skip_fields: list | None = None) -> Any` — (inferred) Pp cast — takes `msg: Any`, `skip_fields: list | None`; returns `Any`.
  - `from toolchemy.utils import pp_cast`
- **function** `to_json(dataclass_container, key_prefix: str = None, exclude: list[str] | None = None) -> dict` — (inferred) To json — takes `dataclass_container`, `key_prefix: str`, `exclude: list[str] | None`; returns `dict`.
  - `from toolchemy.utils import to_json`
- **function** `truncate(s: str, limit: int) -> str` — (inferred) Truncate — takes `s: str`, `limit: int`; returns `str`.
  - `from toolchemy.utils import truncate`

## `toolchemy.utils.cacher`

- **class** `BaseCacher()` — (inferred) Class `BaseCacher` extends ICacher, ICollectable, ABC; methods: collect, create_cache_key, exists, get, hash….
  - `from toolchemy.utils.cacher import BaseCacher`
- **class** `CacherDiskcache(name: str | None = None, cache_base_dir: Optional[str] = None, shards: int = 1, timeout: int = 30, thread_safe: bool = False, disabled: bool = False, log_level: int = 20)` — (inferred) Class `CacherDiskcache` extends BaseCacher; methods: collect, create_cache_key, exists, get, hash….
  - `from toolchemy.utils.cacher import CacherDiskcache`
- **class** `CacherShelve(name: str | None = None, cache_base_dir: Optional[str] = None, disabled: bool = False, log_level: int = 20, enable_thread_safeness: bool = False)` — (inferred) Class `CacherShelve` extends BaseCacher; methods: collect, create_cache_key, exists, get, hash….
  - `from toolchemy.utils.cacher import CacherShelve`

## `toolchemy.utils.cacher.cacher_diskcache`

- **class** `DummyLock()` — (inferred) Class `DummyLock`; methods: acquire, release.
  - `from toolchemy.utils.cacher.cacher_diskcache import DummyLock`

## `toolchemy.utils.cacher.cacher_shelve`

- **function** `testing()` — (inferred) Testing — takes no arguments.
  - `from toolchemy.utils.cacher.cacher_shelve import testing`

## `toolchemy.utils.cacher.common`

- **class** `CacheEntryHasNotBeenSetError(...)` — (inferred) Class `CacheEntryHasNotBeenSetError` extends Exception.
  - `from toolchemy.utils.cacher.common import CacheEntryHasNotBeenSetError`
- **class** `CacheEntrySeemMalformedError(...)` — (inferred) Class `CacheEntrySeemMalformedError` extends Exception.
  - `from toolchemy.utils.cacher.common import CacheEntrySeemMalformedError`
- **class** `CacherInitializationError(...)` — (inferred) Class `CacherInitializationError` extends Exception.
  - `from toolchemy.utils.cacher.common import CacherInitializationError`
- **class** `DummyLock()` — (inferred) Class `DummyLock`.
  - `from toolchemy.utils.cacher.common import DummyLock`

## `toolchemy.utils.datestimes`

- **class** `Seconds(*values)` — (inferred) Class `Seconds` extends float, Enum.
  - `from toolchemy.utils.datestimes import Seconds`
- **function** `current_date_str(date_format: str = '%d-%m-%Y', time_delta_days: int | None = None) -> str` — (inferred) Current date str — takes `date_format: str`, `time_delta_days: int | None`; returns `str`.
  - `from toolchemy.utils.datestimes import current_date_str`
- **function** `current_datetime_str(datetime_format: str = '%d-%m-%Y %H:%M:%S', time_delta_days: int | None = None) -> str` — (inferred) Current datetime str — takes `datetime_format: str`, `time_delta_days: int | None`; returns `str`.
  - `from toolchemy.utils.datestimes import current_datetime_str`
- **function** `current_unix_timestamp() -> int` — (inferred) Current unix timestamp — takes no arguments; returns `int`.
  - `from toolchemy.utils.datestimes import current_unix_timestamp`
- **function** `date_to_str(date_obj: datetime.date, date_format: str = '%d-%m-%Y') -> str` — (inferred) Date to str — takes `date_obj: date`, `date_format: str`; returns `str`.
  - `from toolchemy.utils.datestimes import date_to_str`
- **function** `datetime_to_str(datetime_obj: datetime.datetime, datetime_format: str = '%d-%m-%Y %H:%M:%S') -> str` — (inferred) Datetime to str — takes `datetime_obj: datetime`, `datetime_format: str`; returns `str`.
  - `from toolchemy.utils.datestimes import datetime_to_str`
- **function** `datetime_to_unix_timestamp(datetime_ob: datetime.datetime) -> int` — (inferred) Datetime to unix timestamp — takes `datetime_ob: datetime`; returns `int`.
  - `from toolchemy.utils.datestimes import datetime_to_unix_timestamp`
- **function** `format_str(datetime_str: str, date_format: str, target_datetime_format: str = '%d-%m-%Y %H:%M:%S') -> str` — (inferred) Format str — takes `datetime_str: str`, `date_format: str`, `target_datetime_format: str`; returns `str`.
  - `from toolchemy.utils.datestimes import format_str`
- **function** `seconds_to_time_str(seconds: int | float) -> str` — (inferred) Seconds to time str — takes `seconds: int | float`; returns `str`.
  - `from toolchemy.utils.datestimes import seconds_to_time_str`
- **function** `str_to_date(date_str: str, date_format: str = '%d-%m-%Y') -> datetime.date` — (inferred) Str to date — takes `date_str: str`, `date_format: str`; returns `date`.
  - `from toolchemy.utils.datestimes import str_to_date`
- **function** `str_to_datetime(datetime_str: str, datetime_format: str = '%d-%m-%Y %H:%M:%S') -> datetime.datetime` — (inferred) Str to datetime — takes `datetime_str: str`, `datetime_format: str`; returns `datetime`.
  - `from toolchemy.utils.datestimes import str_to_datetime`
- **function** `str_to_unix_timestamp(datetime_str: str, datetime_format: str = '%d-%m-%Y %H:%M:%S') -> int` — (inferred) Str to unix timestamp — takes `datetime_str: str`, `datetime_format: str`; returns `int`.
  - `from toolchemy.utils.datestimes import str_to_unix_timestamp`
- **function** `unix_timestamp_to_datetime(unix_timestamp: int) -> datetime.datetime` — (inferred) Unix timestamp to datetime — takes `unix_timestamp: int`; returns `datetime`.
  - `from toolchemy.utils.datestimes import unix_timestamp_to_datetime`

## `toolchemy.utils.locations`

- **class** `PathDoesNotExistError(...)` — (inferred) Class `PathDoesNotExistError` extends Exception.
  - `from toolchemy.utils.locations import PathDoesNotExistError`
- **function** `get_external_caller_path(exclude_prefixes=None) -> str` — (inferred) Get external caller path — takes `exclude_prefixes`; returns `str`.
  - `from toolchemy.utils.locations import get_external_caller_path`

## `toolchemy.utils.logger`

- **function** `testing()` — (inferred) Testing — takes no arguments.
  - `from toolchemy.utils.logger import testing`

## `toolchemy.utils.utils`

- **function** `batchize(items: list[typing.Any], batch_size: int) -> list[list[typing.Any]]` — (inferred) Batchize — takes `items: list[Any]`, `batch_size: int`; returns `list[list[Any]]`.
  - `from toolchemy.utils.utils import batchize`
- **function** `bytes_to_str(byte_data: bytes) -> str` — (inferred) Bytes to str — takes `byte_data: bytes`; returns `str`.
  - `from toolchemy.utils.utils import bytes_to_str`
- **function** `normalize_path_str(path_: str) -> str` — (inferred) Normalize path str — takes `path_: str`; returns `str`.
  - `from toolchemy.utils.utils import normalize_path_str`
- **function** `seed_init_fn(x, only_deterministic: bool = False)` — (inferred) Seed init fn — takes `x`, `only_deterministic: bool`.
  - `from toolchemy.utils.utils import seed_init_fn`
- **function** `split_text(text: str, chunk_size: int, chunk_overlap: int) -> list[str]` — (inferred) Split text — takes `text: str`, `chunk_size: int`, `chunk_overlap: int`; returns `list[str]`.
  - `from toolchemy.utils.utils import split_text`

## `toolchemy.vision`

- **class** `Caption(text: str, y: int, font: PIL.ImageFont.FreeTypeFont | None = None, font_name: str | None = None, size: int | None = None, color: tuple | None = None) -> None` — Caption(text: str, y: int, font: PIL.ImageFont.FreeTypeFont | None = None, font_name: str | None = None, size: int | None = None, color: tuple | None = None)
  - `from toolchemy.vision import Caption`
- **class** `ImageProcessor(input_image: str | PIL.Image.Image)` — (inferred) Class `ImageProcessor`; methods: metadata, scale.
  - `from toolchemy.vision import ImageProcessor`
- **function** `add_caption(input_img_path: str, captions: list[toolchemy.vision.caption_overlay.Caption], output_img_path: str | None = None)` — (inferred) Add caption — takes `input_img_path: str`, `captions: list[toolchemy.vision.caption_overlay.Caption]`, `output_img_path: str | None`.
  - `from toolchemy.vision import add_caption`

## `toolchemy.vision.caption_overlay`

- **function** `main()` — (inferred) Main — takes no arguments.
  - `from toolchemy.vision.caption_overlay import main`

## `toolchemy.vision.image`

- **class** `UnknownImageFormatError(...)` — (inferred) Class `UnknownImageFormatError` extends Exception.
  - `from toolchemy.vision.image import UnknownImageFormatError`
